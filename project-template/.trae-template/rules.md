# 项目规则

> 项目级规则引导文件。全局通用规则由 `~/.trae/user_rules/` 加载。

## 引用全局规则

本项目遵循以下全局规则（由 Trae Agent 自动加载）：

- `team-framework.md` — 角色协作框架
- `coding-principles.md` — 通用编码原则
- `output-constraints.md` — Agent 输出约束

## 项目特定规则

在此追加本项目特有的规则和约定。

### 项目结构

```
.
├── .trae/
│   ├── roles/
│   │   └── memory/    # 项目本地角色记忆
│   └── rules.md       # 本文件
├── src/               # 源代码
├── docs/              # 项目文档
└── tests/             # 测试
```

### 命名约定

<!-- 在此定义项目特有的命名约定 -->

### 依赖管理

<!-- 在此说明项目的依赖管理方式 -->
